# 군체 저항도

사용자가 제공한 10개의 생존 상황으로 진행하는 일회용 행사 웹앱입니다. 닉네임 하나로 참여하고, 선택 비율과 군체 저항도를 확인합니다.

- [참여 사이트](https://dhoklim.github.io/colony-resistance/)
- [운영실](https://colony-resistance.dhoklim.chatgpt.site/admin)

## 행사 운영 순서

1. 운영실에서 **이벤트 시작하기**를 누릅니다. 로그인·비밀번호·사전 설정은 필요 없습니다.
2. 운영실의 QR을 게시하거나 참여 링크를 공유합니다. 참가자는 닉네임만 입력하고 대기합니다. 아직 문제가 공개되지 않았다면 질문과 선택지도 보이지 않습니다.
3. **문제 진행**의 버튼 하나를 사용합니다. **1번 문제 공개 → 1번 결과 공개 → 2번 문제 공개 → 2번 결과 공개 → … → 10번 결과 공개** 순서로 버튼 이름이 바뀝니다. 문제를 공개하면 참가자 화면이 자동으로 넘어갑니다. 응답 수를 확인한 뒤 결과를 공개하면 해당 문항의 응답이 즉시 마감되고 비율·점수가 표시됩니다. 다음 문제를 다시 공개할 때까지 직전 결과를 유지하며 참가자용 다음 버튼은 없습니다. 마지막 결과를 공개하면 10문항 완료자의 총점과 CSV 점수도 자동으로 표시됩니다.
4. 10문항 진행 후 **응답 마감하기 → 마감 확정**을 누릅니다. 이때 행사 전체를 마감하고 최종 점수를 확정합니다. 중간에 전체 응답을 마감하면 이후에는 새 답변 없이 남은 결과만 차례로 공개할 수 있으므로, 정상 진행에서는 마지막 결과 공개 후 누르세요.
5. **당첨자 2명 선정 → 추첨 확정**을 누릅니다. 결과는 한 번만 저장되며 새로고침해도 바뀌지 않습니다.
6. 닉네임과 자동 발급된 참가 코드로 당첨자를 확인하고, 학과 공식 인스타그램에 결과를 직접 발표합니다. 필요하면 CSV를 내려받습니다. 자동 게시·관람권 발송 기능은 없습니다.
7. 처음부터 다시 하려면 **행사 다시하기 → 지우고 다시 시작**을 누릅니다. 참가 명단·답변·점수·추첨 결과를 모두 삭제하고 새 참가 접수를 엽니다. 문제 진행도 첫 문제 공개 전의 대기 상태로 돌아갑니다. 이전 기록은 복구할 수 없으므로 필요한 CSV는 먼저 내려받으세요.

동일 닉네임도 참여할 수 있으며 참가 코드로 구분합니다. 참가자는 결과 화면을 캡처해 두면 됩니다. 배포만으로는 기존 기록을 바꾸지 않습니다. 운영자가 다시하기를 확인하면 열린 참가 화면도 새 행사에 맞춰 닉네임 입력 화면으로 돌아갑니다. 같은 행사의 답변 수정이나 재추첨은 지원하지 않습니다.

운영자가 진행을 관리하므로 응답하지 않은 사람이 있어도 다음 단계로 진행할 수 있습니다. 늦게 입장하거나 문항을 놓친 참가자는 현재 열린 문제부터 참여할 수 있지만, 마감된 문항에는 답할 수 없습니다. 미제출 문항은 점수가 없으며 10문항을 모두 완료하지 않으면 추첨 대상에서 제외됩니다. 새로고침해도 서버의 진행 단계를 따릅니다. 여러 운영 창의 같은 단계 요청이나 재시도는 단계를 중복으로 넘기지 않습니다. 기존 행사 기록은 마이그레이션으로 보존하며, 이미 공개된 연속 문항 다음에서 이어갑니다.

## 점수와 추첨 규칙

- 각 문항의 득표수가 적은 순서대로 5·3·1·0점을 줍니다. 0표 선택지도 순위 계산에 포함합니다.
- 같은 득표수는 공동 순위로 처리하고 다음 순위를 건너뜁니다. `[5, 5, 10, 20] → [5, 5, 1, 0]`, `[0, 0, 0, 20] → [5, 5, 5, 0]`입니다.
- 10문항 합계는 0~50점입니다. 진행 중 값은 잠정값이며 응답 마감 때 확정됩니다.
- 중도 이탈자가 이미 제출한 응답도 문항별 분포에 포함하지만 추첨은 10문항 완료자만 대상으로 합니다.
- 최고점 완료자가 2명 이상이면 그중 2명을 무작위로 선정합니다. **단독 최고점이면 해당 참가자를 선정하고 차순위 점수 참가자 중 1명을 추가 선정**하도록 보완했습니다. 원래 기획에 없던 예외 규칙이므로 운영 안내에 명시했습니다.
- 완료자가 1명뿐이면 1명만 선정하고, 0명이면 추첨을 막습니다. 보안 난수와 비복원 추출을 사용합니다.

## 개발 실행

Node.js 22.13 이상과 npm을 사용합니다. 이 작업 환경에서는 Windows Node.js 24로 검증했습니다.

```bash
npm ci
cp .env.example .env
# .env의 로컬 설정을 입력한 뒤 실행
npm run db:migrate
npm run dev
```

`.env`는 Git에서 제외됩니다. Sites 개발 서버가 출력하는 로컬 테스트 로그인 계정만 로컬 `ADMIN_EMAILS`에 등록합니다. `SITE_URL`은 개발 서버에 표시되는 주소와 같아야 합니다. 로컬 테스트 계정을 실제 배포 설정에 넣지 마세요.

```bash
npm test
npm run typecheck
npm run lint
npm run build
```

테스트는 Node test runner, React Testing Library/JSDOM, 격리된 Miniflare D1을 사용합니다. 실제 행사 DB에 테스트 참가자를 넣지 않습니다. 규칙, 입장 대기, 순차 공개, 중복 진행 방지, 공개와 제출 경합, 자동 화면 이동, 동시·중복 제출, 마감 경합, 세션 복원, 접근 통제, 오류 재시도, 10문항 완료와 추첨을 검사합니다. 브라우저 화면 테스트는 별도입니다.

스키마 변경 시 `npm run db:generate`로 SQL을 생성하고 `drizzle/`의 결과를 검토합니다. 로컬 DB에는 `npm run db:migrate`를 실행합니다. 호스팅에는 Sites 패키징·배포 절차로 마이그레이션을 포함합니다. 요청 처리 중 DDL을 실행하지 않습니다.

## 데이터와 공개 범위

- 참가자는 닉네임만 입력합니다. 실명·학번·연락처, 동의 체크박스, 확인 문구 입력은 요구하지 않습니다. 운영실과 CSV에는 닉네임·참가 코드·진행 상황·점수가 표시됩니다.
- 이번 배포는 사용자 승인에 따라 `PUBLIC_ADMIN_ACCESS=true`입니다. 누구나 운영실과 명단·CSV를 보고 시작·문제 공개·결과 공개·마감·추첨·다시하기를 실행할 수 있습니다. 마감·추첨·다시하기의 확인 버튼은 실수 방지용입니다.
- 공개 전에는 참가 API와 운영실 API 모두 문항별 선택 수·비율·배점을 전달하지 않습니다. 운영실에는 전체 응답 수만 표시합니다. 총점·CSV 점수·추첨 결과도 모든 문항을 공개하기 전에는 숨깁니다. 응답 마감만으로 결과가 공개되지는 않습니다.
- 운영실 접근은 기존 요청대로 누구에게나 열려 있으므로 누구나 결과 공개 버튼을 누를 수 있습니다. 공개 버튼을 누르기 전의 결과 노출을 막는 기능이며, 운영자만 버튼을 사용할 수 있게 하는 접근 통제는 아닙니다. 참가 화면에서는 본인이 답변한 문항만 결과를 볼 수 있지만, 이미 공개된 결과는 운영실에서 누구나 확인할 수 있습니다.
- 같은 브라우저에서는 기존 참여 기록을 이어받습니다. 닉네임은 로그인 수단이 아니며 다른 기기의 기록을 가져올 수 없습니다. 다중 브라우저 참여나 실제 학생 신원 확인은 제한하지 않습니다.
- 참가 세션은 30일 유효합니다. 서버에서는 HttpOnly 쿠키, GitHub Pages에서는 브라우저 저장소의 임의 토큰을 사용합니다. DB에는 토큰의 해시만 저장합니다. 행사에서는 동일한 Pages 주소를 안내하세요.
- 입력 검사, 출처 검사, 요청 제한과 중복 제출 방지는 유지합니다. 기존 이름 필드는 닉네임을 저장하는 데 재사용하고, 학번 필드만 마이그레이션으로 제거합니다. 다시하기를 실행할 때만 기존 참가 기록을 삭제합니다. 회차 번호로 중복 초기화 요청과 이전 회차의 제어 요청을 구분해 새 기록이 지워지지 않게 합니다.
- 비공개 모드가 필요하면 `PUBLIC_ADMIN_ACCESS=false`와 실제 운영자의 `ADMIN_EMAILS`를 설정해 다시 배포합니다. 기본값은 비공개이며 목록이 비어 있으면 접근을 거절합니다. 로컬 테스트 계정을 실제 배포에 사용하지 마세요.

## 행사 종료 후 정리

자동 삭제·자동 사이트 종료는 구현되어 있지 않습니다. 경품 지급과 결과 확인이 끝난 뒤 사이트 공개를 종료하고 보관할 필요가 없는 데이터를 직접 정리합니다. 아래 SQL은 권한 있는 DB 관리 도구에서만 실행하며, 기존 기록을 삭제하므로 테스트나 행사 시작 과정에서는 실행하지 마세요.

```sql
DELETE FROM draws;
DELETE FROM answers;
DELETE FROM participants;
DELETE FROM rate_limits;
```

이 작업은 되돌릴 수 없으며 참가자의 결과 재조회도 종료됩니다. 내려받은 CSV와 별도 보관본은 따로 정리해야 합니다. `events.final_counts`에는 선택지별 합계만 남고 행사 상태는 다시 열리지 않습니다.

## 프로젝트 구조

- `app/lib/questions.ts`: 사용자 원문 10문항·40개 선택지.
- `app/lib/rules.ts`: 채점, 추첨, CSV 셀 안전 처리.
- `app/server/service.ts`: D1 저장·집계·마감·추첨.
- `app/server/api.ts`: HTTP 동작, 인증·세션·출처 검사.
- `app/components/`: 참가·운영 화면.
- `db/schema.ts`, `drizzle/`: 스키마와 생성된 마이그레이션.
- `tests/`: 격리된 데이터·API·화면 테스트.
- `.openai/hosting.json`: Sites 식별자와 논리 DB 바인딩만 보관. 비밀값은 넣지 않습니다.

검정·초록 시각 체계와 Lucide 아이콘을 사용합니다. `public/og.png`는 이번 이벤트를 위해 생성한 공유 이미지이며 공식 영화 포스터나 실제 장면이 아닙니다. 문항은 사용자가 제공한 이벤트 시나리오입니다.

## GitHub Pages 배포

- 참여 사이트: https://dhoklim.github.io/colony-resistance/
- 서버·운영실: https://colony-resistance.dhoklim.chatgpt.site/admin
- 소스 저장소: https://github.com/dhoklim/colony-resistance

`github-pages/`는 기존 소개·참여 컴포넌트를 재사용하는 정적 진입점입니다. 해시 경로 `#/participate`로 이동하므로 직접 링크와 새로고침도 동작합니다. 페이지 리디렉션이나 iframe 대체물이 아니며, 참여 화면은 GitHub Pages에서 실행됩니다.

`npm run build:pages`로 `dist-pages/`를 만듭니다. main 브랜치에 푸시하면 `.github/workflows/pages.yml`이 빌드한 정적 파일만 GitHub Pages에 배포합니다. DB와 서버 코드는 정적 배포물에 포함하지 않습니다. 서버 변경은 기존 Sites 빌드·배포 절차가 별도로 필요합니다.

서버 환경 설정:

- `PARTICIPANT_ORIGIN=https://dhoklim.github.io`: 허용하는 참여 화면 출처. 와일드카드 CORS를 사용하지 않습니다.
- `PARTICIPATION_URL=https://dhoklim.github.io/colony-resistance/#/participate`: 운영실에서 만드는 행사 QR의 주소.
- `SITE_URL`: 기존 서버의 정확한 HTTPS 주소를 유지합니다.

Pages 출처의 CORS 요청은 참가용 API에만 허용합니다. 관리자 화면은 기존 서버 주소에서 열며, 공개 운영 모드에서는 관리자 API도 로그인을 요구하지 않습니다. 공개 여부는 서버 설정으로만 결정하며 브라우저 요청으로 변경할 수 없습니다. Pages가 저장하는 참가 토큰은 본인 답변 제출 권한이 있으므로 URL·로그·공유 메시지에 넣지 마세요. GitHub Pages의 같은 계정 내 다른 프로젝트도 동일한 브라우저 출처를 공유하므로, 신뢰할 수 없는 스크립트를 해당 계정의 Pages에 게시하지 마세요.
